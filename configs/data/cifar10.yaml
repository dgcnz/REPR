defaults: 
- train_transform: cifar10

_target_: src.data.hf_datamodule.HFDataModule
dataset_name: "uoft-cs/cifar10"
img_key: "img"
batch_size: 16 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 0
pin_memory: True
val_fraction: 0.1
