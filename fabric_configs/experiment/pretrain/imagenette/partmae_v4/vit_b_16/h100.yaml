# @package _global_
defaults:
  - pretrain/imagenette/partmae_v4/vit_b_16/_base

compile: False

trainer:
  max_epochs: 200
  accelerator: gpu
  strategy: ddp
  num_nodes: 1
  devices: 2

scheduler:
  warmup_t: 10

train_dataloader:
  batch_size: 512
  num_workers: 16
