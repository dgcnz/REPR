# @package _global_
defaults:
  - pretrain/imagenette/partmae_v6/vit_b_16/_base

compile: False

trainer:
  max_epochs: 200
  accelerator: gpu
  # limit_train_batches: 128
  accumulate_grad_batches: 8

scheduler:
  warmup_t: 6250
  t_initial: 125000

train_dataloader:
  batch_size: 128
  num_workers: 8
